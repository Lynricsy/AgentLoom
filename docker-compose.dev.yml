version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:v1.14.0
    ports:
      - '6333:6333'
      - '6334:6334'
    volumes:
      - qdrant_data:/qdrant/storage
    environment:
      QDRANT__TELEMETRY_DISABLED: 'true'

volumes:
  qdrant_data:
